# 🀄 四川双打竞技麻将 - 前后端对齐规范 (API & WebSocket Spec)

这份文档旨在为 Cocos (或其他游戏前端引擎) 开发者提供详尽的后端数据结构、WebSocket 消息协议以及 REST API 接口定义，以确保前后端颗粒度完全对齐。

## 1. 核心业务概念与数据结构

### 1.1 牌 (Tile) 定义
游戏共使用 **108** 张牌（万、筒、条，各36张），无风向和字牌。在网络传输中，统一使用唯一的 `tileId` (0~26) 进行传递。

- **万 (Wan) 1-9**: `tileId` = 0 ~ 8
- **筒 (Tong) 1-9**: `tileId` = 9 ~ 17
- **条 (Tiao) 1-9**: `tileId` = 18 ~ 26

花色索引 (Suit Index)：
- `0` = 万
- `1` = 筒
- `2` = 条

### 1.2 座位 (Seat) 定义
在一桌合规的四人局中，`seatIndex` 严格取值为 `0`, `1`, `2`, `3`。
在 Cocos 渲染层面，可以将自身固定在底部，依据 `当前玩家的 seatIndex` 对计算其他三家的相对位置得出他们的 UI 渲染位（如 Bottom, Right, Top, Left）。

---

## 2. 🔌 WebSocket 交互协议

**连接地址**: `ws://[Host]:[Port]/ws/game?userId={您的UserID}`
> *注意: 建立连接时必须在 URL 参数中携带合法的 userId。*

### 2.1 统一消息封装格式
前后端交互的所有 WebSocket 数据格式都必须是严格的 JSON 字符串结构：
```json
{
  "type": "C_DISCARD",       // 消息类型，客户端发出的必须以 C_ 开头，服务端发给客户端的以 S_ 开头
  "roomId": "887766",        // 当前房间号 (可空，具体看协议)
  "userId": 12345,           // 您的用户ID (可空，具体看协议)
  "data": {                  // 具体的业务负载数据
    "tileId": 14
  }
}
```

### 2.2 客户端 -> 服务端 (C2S) 指令集

| 消息类型 (type) | data 负载所需字段 | 描述 |
| --- | --- | --- |
| `C_JOIN_ROOM` | *(空)* | 加入房间。须同级提供 `roomId` 和 `userId`。断线重连也会使用此指令，后端会自动下发全量状态恢复游戏。 |
| `C_LEAVE_ROOM`| *(空)* | 玩家完全退出/离开当前房间。若房主离开且房间还有其他人，会自动转让房主权限而不解散。 |
| `C_START_GAME`| *(空)* | 房主强制开始游戏（跳过所有人的准备阶段）。 |
| `C_SELECT_MISS_SUIT` | `{"suitIndex": Int}` | 玩家选择定缺花色 (0=万, 1=筒, 2=条) |
| `C_DISCARD` | `{"tileId": Int}` | 玩家出牌 |
| `C_PENG` | *(空)* | 玩家声明碰牌 |
| `C_GANG` | `{"gangType": "BU"或"MING", "tileId": Int}`| 玩家声明明杠 / 补杠 |
| `C_AN_GANG` | `{"tileId": Int}` | 玩家声明暗杠 |
| `C_HU` | `{"isSelfDraw": Boolean}` | 玩家声明胡牌 (点炮 or 自摸) |
| `C_PASS` | *(空)* | 玩家选择过/跳过 (放弃碰/杠/胡) |
| `C_CHAT` | `{"message": String, "senderName": String, "senderId": Long}` | 发送房间内聊天/快捷语 |
| `C_PING` | *(空)* | 心跳包，建议前端每隔 20~30秒 发送一次保持连接活跃 |
| `C_SELECT_SEAT` | `{"seatIndex": Int}` | 玩家在准备阶段选择空座 |
| `C_READY` | `{"isReady": Boolean}` | 玩家在准备阶段切换准备状态 |
| `C_INVITE_FRIEND` | `{"targetUserId": Long}` | 邀请好友加入当前房间。须同级提供 `roomId` 和 `userId`。 |


### 2.3 服务端 -> 客户端 (S2C) 广播与推送

| 消息类型 (type) | data 负载数据结构说明 | 描述 |
| --- | --- | --- |
| `S_ROOM_STATE` | `{"roomId": String, "status": String, "players": [...]}` | 房间状态全量同步，`players` 数组包含各玩家的 `seatIndex`, `userId`, `nickname`, `online` 等状态 |
| `S_PLAYER_STATUS` | `{"seatIndex": Int, "online": Boolean, "nickname": String}` | 房间内个别玩家上下线通知 |
| `S_GAME_START` | `{"handTiles": [Int, Int...], "dealerSeat": Int, "wallCount": Int}` | 游戏正式开始，下发**您自己**的初始手牌、庄家座位以及牌墙剩余张数 |
| `S_SELECT_MISS_SUIT`| *(空)* | 广播定缺阶段开始，前端弹出定缺UI |
| `S_MISS_SUIT_RESULT`| `[{"seatIndex": 0, "suitIndex": 1}, ...]` | 所有人定缺完毕后广播最终每个人的定缺花色 |
| `S_DRAW` | `{"tileId": Int, "tingTiles": [Int, ...]}` | 只有摸牌的本人会收到，通知其摸到了哪张牌。`tingTiles` 为当前可胡牌的牌 ID 列表（空数组表示未听牌）。 |
| `S_DISCARD` | `{"seatIndex": Int, "tileId": Int}` | 广播某人打出了一张牌 |
| `S_ACTION_OPTIONS` | `{"options": ["PENG", "GANG...", "HU", "PASS"], "targetTile": Int...}`| 服务端询问玩家是否要对当前被打出的牌 (或摸出的牌) 进行操作 |
| `S_PENG` | `{"seatIndex": Int, "tileId": Int, "fromSeat": Int}` | 广播某人碰牌 |
| `S_GANG` | `{"seatIndex": Int, "tileId": Int, "gangType": String}` | 广播某人杠牌 |
| `S_GANG_DRAW`| `{"tileId": Int}` | 杠牌后的补牌通知（仅当事人收到） |
| `S_HU` | `{"seatIndex": Int, "isSelfDraw": Boolean, "winType": String}` | 广播某人胡牌。`winType` 为番型描述字符串，例如 `"龙七对+清一色+自摸"`，前端可直接展示。 |
| `S_COUNTDOWN` | `{"seatIndex": Int, "seconds": Int}` | 广播当前出牌方的剩余倒计时秒数（默认 20 秒），超时后服务端自动出牌。 |
| `S_ROUND_RESULT` | `{"scores": [...], "details": [...]}` | 本局结算，包含底分、番数结算和各方得分明细 |
| `S_GAME_OVER` | *(待定)* | 整个大局(如4局/8局)打完后的最终结算 |
| `S_CHAT` | `{"seatIndex": Int, "nickname": String, "message": String, "senderId": Long}` | 广播有人发送的聊天消息 |
| `S_ERROR` | `{"code": Int, "message": String}` | 服务端拒绝某项操作时的报错文本，Cocos 可利用此字段弹出 Toast 提示 |
| `S_PONG` | `{"time": Long}` | 服务端返回的心跳响应 |
| `S_ROOM_INVITE` | `{"roomId": String, "roomName": String, "inviterId": Long, "inviterNickname": String}` | 服务端向被邀请玩家推送房间邀请通知，前端可弹出「xxx 邀请你加入房间」的提示。 |
| `S_SPECTATE_INIT` | `{"players": [...], "discardPiles": [[Int...], ...], "melds": [[...], ...], "scores": [Int, ...], "round": Int, "bankerSeat": Int, "phase": String, "remaining": Int}` | 观战者进入对局中房间时推送的当前桌面公开状态（不含任何玩家手牌）。`phase` 取值：`PLAYING`/`MISS_SUIT`/`WAITING`。 |

---

## 3. 🌐 RESTFul HTTP API

所有前端在进入 Cocos 游戏大厅前 (或在大厅内) 需要用到的常规 API 列表 (均挂载于 `http://[Host]:[Port]/api`)：

> **统一响应格式**：所有接口均返回 JSON，成功时含 `"success": true`，失败时含 `"success": false, "msg": "错误原因"`。

---

### 用户模块 (`/api/user/*`)

#### `POST /user/register` — 注册
```json
// Request Body
{ "username": "testuser", "password": "Pass@123", "nickname": "测试用户" }
// Response (success)
{ "success": true, "userId": 1, "nickname": "测试用户" }
```

#### `POST /user/login` — 登录
```json
// Request Body
{ "username": "testuser", "password": "Pass@123" }
// Response (success)
# 🀄 四川双打竞技麻将 - Cocos 对齐文档

这份文档是给 Cocos 开发者看的接入说明，目标不是讲规则，而是把“界面应该怎么走、请求该怎么发、服务端会回什么、哪些字段必须保留”说清楚。

## 1. 开发目标

### 1.1 前端职责
- 维护大厅、房间准备页、对局页三个状态。
- 用 HTTP 拉取初始数据，用 WebSocket 驱动实时同步。
- 只负责展示和发送指令，不在前端重写牌型与回合状态机。

### 1.2 后端职责
- 维护房间、座位、对局状态、倒计时与结算。
- 校验所有操作是否合法。
- 人机会被当作普通座位玩家处理，Cocos 不需要区分它们是否是 AI，除了 UI 标签。

### 1.3 推荐接入顺序
1. 登录拿到 `userId`。
2. 建立 WebSocket：`ws://[Host]:[Port]/ws/game?userId={userId}`。
3. 通过 HTTP 获取房间详情或房间列表。
4. 进入房间后发送 `C_JOIN_ROOM`。
5. 根据 `S_ROOM_STATE`、`S_GAME_START`、`S_DRAW`、`S_ACTION_OPTIONS` 驱动 UI。

---

## 2. 基础数据约定

### 2.1 牌 ID
网络里统一使用 `tileId`，范围是 `0~26`。

- `0~8`：万 1~9
- `9~17`：筒 1~9
- `18~26`：条 1~9

花色索引：
- `0` = 万
- `1` = 筒
- `2` = 条

### 2.2 座位 ID
- `seatIndex` 取值 `0~3`
- 推荐 Cocos 固定本家在底部，其余座位按顺时针映射到右、上、左

### 2.3 房间状态
- `WAITING`：等待入座/准备
- `READY`：4 人就座且准备完成
- `PLAYING`：对局中
- `FINISHED`：整房比赛结束

---

## 3. WebSocket 协议

### 3.1 通用消息格式
```json
{
  "type": "C_DISCARD",
  "roomId": "R123",
  "userId": 10001,
  "data": {
    "tileId": 12
  }
}
```

### 3.2 客户端 -> 服务端

| type | data | 说明 |
| --- | --- | --- |
| `C_JOIN_ROOM` | 空 | 进入房间大厅；观战重连也走这个入口 |
| `C_CHOOSE_SEAT` | `{ "seatIndex": 0 }` | 选择座位 |
| `C_LEAVE_SEAT` | 空 | 从座位起身回到大厅 |
| `C_READY` | 空 | 切换准备状态 |
| `C_START_GAME` | 空 | 房主开始游戏 |
| `C_ADD_BOT` | 空或 `{ "seatIndex": 0 }` | 房主添加人机；不传则自动找空位 |
| `C_SELECT_MISS_SUIT` | `{ "suitIndex": 0 }` | 定缺，0=万 1=筒 2=条 |
| `C_DISCARD` | `{ "tileId": 12 }` | 出牌 |
| `C_PENG` | 空 | 碰 |
| `C_CHI` | `{ "consumeTileIds": [10, 11] }` | 吃 |
| `C_GANG` | `{ "gangType": "MING"/"BU", "tileId": 12 }` | 明杠 / 补杠 |
| `C_AN_GANG` | `{ "tileId": 12 }` | 暗杠 |
| `C_HU` | `{ "isSelfDraw": true/false }` | 胡牌 |
| `C_PASS` | 空 | 过 |
| `C_CHAT` | `{ "message": "..." }` | 房间聊天 |
| `C_PING` | 空 | 心跳 |
| `C_LEAVE_ROOM` | 空 | 离开房间 |
| `C_DISBAND_ROOM` | 空 | 房主解散房间 |
| `C_INVITE_FRIEND` | `{ "targetUserId": 10002 }` | 邀请好友 |

### 3.3 服务端 -> 客户端

#### 房间与座位类
| type | data | 说明 |
| --- | --- | --- |
| `S_ROOM_STATE` | `{ roomId, roomName, status, creatorId, currentRound, maxRounds, baseScore, allowChi, enableFengYu, seats, lobbyUsers, allReady }` | 房间全量同步。注意不同入口可能字段略有差异，Cocos 需容错读取。`seats` 中包含 `isBot`。 |
| `S_SEAT_CHANGED` | `{ seatIndex, action, userId, nickname, isBot? }` | 入座/起身广播，`action` 为 `SIT`、`STAND`、`LEAVE` |
| `S_READY_CHANGED` | `{ seatIndex, userId, ready }` | 准备状态变化 |
| `S_PLAYER_STATUS` | `{ seatIndex, online, nickname }` | 玩家上线/离线 |
| `S_ROOM_INVITE` | `{ roomId, roomName, inviterId, inviterNickname }` | 好友邀请通知 |
| `S_ROOM_DISBANDED` | `{ roomId, reason }` | 房间解散 |

#### 对局阶段类
| type | data | 说明 |
| --- | --- | --- |
| `S_GAME_START` | `{ seatIndex, bankerSeat, round, maxRounds, handTiles }` | 只下发自己的手牌 |
| `S_SELECT_MISS_SUIT` | `{ message }` | 进入定缺阶段 |
| `S_MISS_SUIT_RESULT` | `{ seatIndex, suitIndex }` | 某位玩家定缺结果广播 |
| `S_DRAW` | `{ seatIndex, tile, remaining, actions, canAnGangIds, buGangTileId?, tingTiles }` | 只发给摸牌者；`actions` 里可能包含 `HU`、`AN_GANG`、`BU_GANG` |
| `S_COUNTDOWN` | `{ seatIndex, seconds }` | 当前出牌倒计时 |
| `S_ACTION_OPTIONS` | `{ seatIndex, actions, chiOptions? }` | 提示某玩家可响应当前弃牌 |
| `S_DISCARD` | `{ seatIndex, tileId, tileName }` | 出牌广播 |
| `S_PENG` | `{ seatIndex, tileId, fromSeat }` | 碰牌广播 |
| `S_CHI` | `{ seatIndex, tileId, fromSeat, consumeTileIds }` | 吃牌广播 |
| `S_GANG` | `{ seatIndex, tileId, gangType }` | 杠牌广播，`gangType` = `MING`/`AN`/`BU` |
| `S_HU` | `{ seatIndex, winTile, isSelfDraw, score, fromSeat, winType }` | 胡牌广播 |
| `S_ROUND_RESULT` | `{ round, players }` | 单局结算 |
| `S_GAME_OVER` | `{ players }` | 全局结束 |

#### 系统类
| type | data | 说明 |
| --- | --- | --- |
| `S_CHAT` | `{ seatIndex, nickname, message }` | 聊天广播 |
| `S_ERROR` | `{ code, message }` | 统一错误返回 |
| `S_PONG` | `{ time }` | 心跳响应 |
| `S_SPECTATE_INIT` | `{ round, bankerSeat, phase, remaining, players }` | 进入进行中房间时的观战初始化 |

---

## 4. HTTP API

统一前缀：`http://[Host]:[Port]/api`

### 4.1 用户相关
- `POST /user/register`
- `POST /user/login`
- `GET /user/{userId}`
- `PUT /user/{userId}/nickname`
- `GET /user/leaderboard?top=10`
- `POST /user/friend/add`
- `GET /user/{userId}/friends`
- `GET /user/{userId}/friend-requests`
- `POST /user/friend/accept/{requestId}?userId=xxx`
- `POST /user/friend/reject/{requestId}?userId=xxx`
- `GET /user/{userId}/records?limit=20`

### 4.2 房间相关
- `POST /room/create`
- `GET /room/list`
- `GET /room/{roomId}`
- `DELETE /room/{roomId}?creatorId=xxx`
- `GET /room/{roomId}/records`
- `GET /room/{roomId}/replay?limit=2000`
- `GET /room/{roomId}/cached`

### 4.3 创建房间建议参数
```json
{
  "creatorId": 1,
  "roomName": "王牌对决",
  "baseScore": 1,
  "maxRounds": 4,
  "allowChi": true,
  "enableFengYu": true
}
```

### 4.4 房间详情返回重点字段
```json
{
  "success": true,
  "roomId": "R123",
  "roomName": "王牌对决",
  "status": "WAITING",
  "creatorId": 1,
  "currentRound": 0,
  "maxRounds": 4,
  "baseScore": 1,
  "allowChi": true,
  "enableFengYu": true,
  "allReady": false,
  "seats": [
    { "seatIndex": 0, "occupied": true, "userId": 1, "nickname": "玩家A", "team": 0, "online": true, "ready": false, "isBot": false },
    { "seatIndex": 1, "occupied": true, "userId": 900000001, "nickname": "AI-1", "team": 1, "online": true, "ready": true, "isBot": true }
  ],
  "lobbyUsers": [
    { "userId": 2, "nickname": "玩家B" }
  ]
}
```

---

## 5. Cocos 接入建议

### 5.1 视图拆分
- 登录/大厅场景：登录、房间列表、好友与邀请。
- 房间准备场景：座位、准备按钮、开始按钮、添加人机、聊天。
- 对局场景：手牌、弃牌河、倒计时、可执行操作面板。

### 5.2 对局状态机
- `WAITING`：展示房间和座位，不进入牌局。
- `SELECTING_MISS_SUIT`：弹出定缺面板，只允许定缺。
- `PLAYING`：轮到自己摸牌/出牌。
- `REACTING`：别人打牌后，展示碰/吃/杠/胡/过。
- `SETTLING`：结算面板。
- `GAME_OVER`：全局结算。

### 5.3 手牌与牌面映射
- `tileId` 用于逻辑判断。
- 显示时按 `tileId` 映射到美术资源。
- `S_DRAW` 回来时，只更新当前玩家手牌；`S_DISCARD`/`S_PENG`/`S_CHI`/`S_GANG` 更新公开区。

### 5.4 人机提示
- `seats[].isBot = true` 时，UI 标注为 AI。
- 人机在后端自动选择定缺、出牌和响应，Cocos 不需要本地模拟 AI。

---

## 6. 约束与注意点

- 不要把任何“能否胡、能否碰、能否吃”的判定写死在 Cocos。
- `S_ACTION_OPTIONS` 才是唯一可信的响应入口。
- `S_DRAW` 只给本人，别广播到全房间。
- `S_SPECTATE_INIT` 不包含任何人的手牌，观战页只能展示公开信息。
- 如果字段缺失，Cocos 要做容错，不要因为额外字段或少量字段差异直接报错。

---

## 7. 开发检查清单

1. 能进入房间并实时刷新座位状态。
2. 能正常准备、开始、定缺、出牌。
3. 能正确处理 `S_ACTION_OPTIONS`，并显示碰/吃/杠/胡/过。
4. 能显示 `S_DRAW`、`S_DISCARD`、`S_PENG`、`S_CHI`、`S_GANG`。
5. 能显示人机座位并允许房主添加人机。
6. 能在对局结束后显示 `S_ROUND_RESULT` 和 `S_GAME_OVER`。

---

## 8. 备注

- 当前后端已经支持吃牌和人机自动行动。
- 房间层的 `allowChi` 开关会影响是否允许吃牌。
- 对局记录与房间快照现在支持 Redis 存储，前端无需关心存储实现。
