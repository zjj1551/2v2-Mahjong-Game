# 启动 / 部署 后端服务器模板

## 1. 构建

```bash
cd mahjong-server
mvn -q -DskipTests compile
```

如需生成可运行包：

```bash
mvn clean package -Dmaven.test.skip=true
```

## 2. 启动

先配置数据库和 Redis 环境变量，再启动 jar：

```bash
export DB_URL='jdbc:mysql://127.0.0.1:3306/mahjong_db?createDatabaseIfNotExist=true'
export DB_USER=root
export DB_PASSWORD=root123
export REDIS_HOST=127.0.0.1
export REDIS_PORT=6379

nohup java -jar target/mahjong-server-1.0.0.jar > ../mahjong-server.log 2>&1 &
```

## 3. 验证

```bash
curl -I http://127.0.0.1:8080 | head -n 1
tail -n 50 ../mahjong-server.log
```

## 备注

- 如果启动失败，优先检查 `DB_URL`、`DB_USER`、`DB_PASSWORD`。
- 如果只改了后端 Java 代码，通常先重新打包，再重启 jar。
- 如果只改了前端静态资源，一般不需要重启后端。